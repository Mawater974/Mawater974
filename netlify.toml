[build]
  command = "npm install && npm run build"
  publish = "dist"
  
[dev]
  command = "npm run dev"
  port = 3000
  publish = "dist"

[context.production.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--version"
  NPM_FLAGS = "--legacy-peer-deps"
